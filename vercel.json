{
  "version": 2,
  "builds": [
    {
      "src": "new/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/client" }
    }
  ],
  "routes": [
    { "src": "^/$", "dest": "/new/" },
    { "src": "^/(.*)$", "dest": "/new/$1" }
  ]
}
